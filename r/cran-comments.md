## R CMD check results

The package was checked on GitHub Actions with current R release and R-devel.
Both ordinary checks report:

    0 errors | 0 warnings | 0 notes

`R CMD check --as-cran` reports:

    0 errors | 0 warnings | 1 note

The remaining note is the expected `New submission` note.

## Downstream dependencies

There are currently no downstream dependencies because this is a new package.
